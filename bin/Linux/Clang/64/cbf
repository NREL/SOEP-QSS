#!/bin/bash
# Clang C++ Build Fast

if [ -f $1.cc ]; then
  CxxMainSource=$1.cc
  OutputSpec="-o $1"
  shift
fi

clang++ -pipe -std=c++11 -pedantic -Wall -Wextra -Winline -ffor-scope -fmessage-length=0 -m64 -march=native -DNDEBUG -Ofast -funroll-loops -s $CxxMainSource $@ $OutputSpec
